This server is powered by Mongodb. If you don't have it installed, run the following: 

```
brew install mongodb
brew services start mongodb
```


Start this server with:

`npm start`

Access it at `localhost:8080`
